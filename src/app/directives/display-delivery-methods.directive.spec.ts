import { DisplayDeliveryMethodsDirective } from './display-delivery-methods.directive';

describe('DisplayDeliveryMethodsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayDeliveryMethodsDirective();
    expect(directive).toBeTruthy();
  });
});
